## Deep Research Request: FiAi（A股 AI 研究 + 策略回测 + 模拟盘执行）
我在做一个面向新手的 A 股 AI 辅助投研与策略系统：用 `akshare` 获取日线/分钟线数据，同时抓取并总结新闻/财报/公告/研报，输出“可解释”的交易信号与风险提示，并提供尽可能贴近真实交易规则的回测与策略验证能力。  
目标是做成可长期迭代的工程化产品（先 Web，再 App，最后小程序）。我有现成项目：前端在 `E:\01_Project\FiAi\fronted`，后端在 `E:\01_Project\FiAi\backend`。UI 风格希望参考 `finscope` 这种现代信息密度高的界面。  
硬约束：最大回撤 \( \le 25\% \)（以回测/模拟盘可验证的口径定义），并追求“稳定”表现（请给出可量化稳定性指标集合，而不是一句话）。

### 重要边界（必须遵守）
- 不提供具体个股“买/卖”投资建议，不承诺盈利；系统输出定位为“研究与决策支持 + 可配置策略 + 回测/模拟盘验证”。
- 数据源与使用需考虑合规与授权（尤其是公告/研报/新闻抓取、行情频率与商用许可）。

---

## 核心问题（请按 2025 最佳实践给出可落地方案）
### 1) 端到端产品形态与信息架构
- Web MVP 应包含哪些核心页面与工作流来覆盖：选股 → 盯盘 → 复盘 → 策略验证/回测 → 规则生成？
- 如何做到“新手可用但不误导”：默认模式、风险提示层级、强制解释、反过拟合提醒等。
- 参考 `finscope` 风格，给出组件与布局建议（例如多面板、可拖拽、指标叠加、信号时间线、事件驱动标注）。

### 2) 数据层（A股：日线 + 分钟线 + 事件文本）
- 用 `akshare` 做行情数据：推荐的数据清洗、缺失值处理、复权处理（前复权/后复权/不复权）、分钟线对齐的工程流程。
- 文本数据（新闻/财报/公告/研报）：可靠来源选择、抓取策略、去重/实体识别（公司/行业/事件）、时间对齐到 K 线的方法。
- 建议的数据存储与索引（时序 + 文本）：表结构/索引策略/冷热分层/增量更新/回放能力。

### 3) 策略与信号体系（可解释优先）
请给出至少 3 条“可解释、可回测、适合新手理解”的策略路线，并说明它们适用/不适用的市场环境：
- 技术指标类（趋势/均值回归/突破/波动率等）
- 事件驱动类（公告/财报关键项变化、重大事件）
- 组合与风控（仓位、止损止盈、分散、最大回撤控制）
同时回答：
- 如何把“LLM 总结的文本观点”安全地转成可回测规则，避免拍脑袋？
- 如何输出解释：每个信号必须包含“触发因素、关键证据、历史相似样本、风险点、失效条件”。

### 4) 回测引擎：要做到什么“级别”才靠谱？
我不清楚回测的“级别”划分。请你给出分级（从简到强）并明确每级包含什么，以及我该选哪一级作为第一阶段目标：
- 撮合规则（开盘/收盘/下一根 K 线成交、限价单/市价单、成交量约束）
- 交易成本（手续费、印花税、滑点模型）
- A股细节（涨跌停、停牌、除权除息、ST、交易时段）
- 评估指标（年化、最大回撤、夏普、卡玛、回撤修复时间、换手率、容量/冲击成本）
- 防过拟合方法（walk-forward、时间切分、样本外、参数稳定性、蒙特卡洛）

### 5) 风控目标：最大回撤 ≤ 25% 的工程化实现
- 给出可执行的风控层设计：事前（选股过滤/仓位限制）+ 事中（止损/减仓/风控熔断）+ 事后（归因与复盘）。
- 如何在回测与模拟盘中用同一套风控代码，避免“回测好看、实盘失真”。
- 如何定义“稳定”：建议一组硬指标阈值（例如 3 年滚动窗口、不同市场阶段、月度胜率/亏损集中度等）。

### 6) 系统架构与工程落地（结合我现有前后端）
- 推荐整体架构（数据采集、特征/指标计算、策略服务、回测服务、信号服务、告警/通知、可视化）。
- 推荐语言/服务划分：我会 `python / golang / react / vue / java`。请给出最省钱且可维护的组合（例如 Python 做数据与回测，Go 做 API 网关与任务调度等）。
- 任务调度与异步：分钟线更新、文本抓取、回测任务、缓存策略、幂等与重试。
- 输出一份“模块清单 + 接口草案 + 数据流图”（文字描述即可），并说明如何渐进式迭代，不推倒重来。

### 7) 模拟盘与执行（先模拟，后可扩展实盘）
- 先做模拟盘：如何实现一个“事件驱动的模拟撮合器”，支持回放与复盘。
- 未来要接券商/第三方交易接口时，架构上如何提前留好抽象层（不绑定某一家）。

### 8) 成本与免费优先
- 我有 `DeepSeek API Key`，其他尽量免费或性价比高。
- 请给出分层成本方案：纯免费/极低成本/可付费提升（数据质量、模型效果、部署监控）。

---

## 交付物（请按清单输出）
- 一份“从 0 到 1”的产品路线图（按里程碑：Web MVP → 强化回测 → 模拟盘 → App → 小程序）
- 回测“分级标准”与我应该采用的目标级别
- 3–5 条可解释策略模板（含入场/出场/仓位/风控/失效条件）
- 数据方案（行情 + 文本）与对齐方式
- 架构与模块划分（含 API 草案与数据表草案）
- 过拟合与评估的最低可行方法（必须能自动化跑）
- UI/信息架构建议（对标 finscope 风格的组件与布局）

---

## AI 协作方式（你在回答时可按角色分段）
- “研究架构师”：系统分层、模块与接口
- “量化工程师”：回测分级、撮合、成本、风控、评估
- “NLP/LLM 工程师”：公告/研报/新闻解析与可回测规则化
- “前端产品设计”：finscope 风格信息架构与可视化

---

## 如何获得最好结果（平台建议）
- `DeepSeek`：用来做方案推演、模块拆分、生成工程骨架与代码草案（成本低）
- `AI Studio (Gemini)`：适合做“长上下文的全面调研与对比”（免费渠道多）
- `Claude`：适合做架构与代码审阅（若有条件再用）
- `ChatGPT`：适合做权衡取舍与迭代提示词（可做交叉验证）

把以上提示词整体复制到你选的平台即可；如果你希望我下一步直接开始“对你的现有 `fronted/backend` 项目做架构体检并给出迭代计划”，我可以立刻基于代码结构往下推进。
